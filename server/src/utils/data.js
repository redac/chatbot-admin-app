let chatbots = {};
let interfaces = ['Web', 'Discord'];
let brains = {};

const data = {
  chatbots: {},
  interfaces: ['Web', 'Discord'],
  brains: {},
};

const INIT_PORT = 4001;

module.exports = { data, INIT_PORT };
